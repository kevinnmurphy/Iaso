 <h2> All of the Meals </h2> 

 <%= link_to 'Create Meal', new_meal_path(@meal) %>

<ul>
  <% @meals.each do |meal| %>
    <li><%= link_to "#{meal.name} (#{meal.pretty_date})", meal_path(meal) %></li>

  <% end %>
</ul>
